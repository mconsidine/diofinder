# diofinder Python dependencies installed via pip wheels.
#
# Heavy compiled libraries (numpy, scipy, Pillow, flask, picamera2)
# come from apt and are visible to the venv via --system-site-packages.
#
# olive-solve's tetra3-py wheel is installed separately by install.sh
# (maturin build + pip install from the local wheel).

smbus2
